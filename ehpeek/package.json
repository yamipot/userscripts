{
  "name": "ehpeek",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.8.3",
    "unocss": "^66.7.5"
  },
  "dependencies": {
    "preact": "^10.29.7"
  }
}
